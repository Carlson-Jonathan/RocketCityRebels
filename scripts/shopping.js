// Quantity oninput change, verify that input is valid
// Maybe not oninput, maybe on form submit/add to cart? Or, just add a selector with a max of the available quantity
function availableQty(qty, inputId) {
    var input = document.getElementById(inputId).value;

}